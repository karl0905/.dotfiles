#!/bin/bash

set -euo pipefail

source_file="$HOME/.config/omarchy/current/theme/walker.css"
target_file="$HOME/.config/walker/themes/omarchy-default/colors.css"

if [[ -f "$source_file" ]]; then
  mkdir -p "$(dirname "$target_file")"
  cp "$source_file" "$target_file"
fi
